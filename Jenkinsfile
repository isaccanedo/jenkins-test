pipeline {
     agent any
     stages {
         stage('build') {
             steps {
                 echo 'Building...'
             }
         }

          stage('deploy to staging') {
             steps {
                 echo 'Deploying...'
             }
         }
          stage('deploy to hurz') {
             steps {
                 echo 'Deploying...'
             }
         }
          stage('deploy to test') {
             steps {
                 echo 'Deploying...'
             }
         }
          stage('deploy to dev') {
             steps {
                 echo 'Deploying...'
             }
         }
     }
 }
